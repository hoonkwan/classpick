# 시트 자동 동기화 설정 가이드

클래스픽은 **Google Sheet "교육in시간표"를 ground truth(데이터의 진실)** 로 사용합니다.
GitHub Actions가 **48시간마다** 시트를 읽어 `db/courses.json`을 갱신하고, Vercel이 자동 배포합니다.

---

## 1. 시트 공개 설정 (대표님 1회만)

> 시트가 "링크 있는 누구나 보기" 또는 "웹에 게시" 상태여야 합니다.
> 둘 다 권장 — "웹에 게시" 쪽이 더 안정적입니다.

### 옵션 A — 링크 공유 (가장 간단)

1. 시트 우측 상단 **공유** 클릭
2. **"링크가 있는 모든 사용자에게 공개"** 로 변경
3. 권한 **뷰어**(보기 전용)
4. 저장

### 옵션 B — 웹에 게시 (더 안정적)

1. 시트 **파일 → 공유 → 웹에 게시**
2. 게시 대상 **"전체 문서"**, 형식 **"웹페이지"**
3. **게시 시작** 클릭
4. 팝업이 뜨면 **확인**

이렇게 하면 다음 URL 패턴으로 누구나 CSV를 가져올 수 있습니다:

```
https://docs.google.com/spreadsheets/d/{시트ID}/gviz/tq?tqx=out:csv&sheet={탭이름}
```

---

## 2. 8개 탭 이름 체크리스트

`scripts/sync-from-sheet.mjs`의 `TABS` 배열은 다음 이름과 **정확히** 일치해야 합니다:

| # | 탭 이름 (정확한 한글) | 기본 area | 기본 term |
|---|---|---|---|
| 1 | `(센텀) 5월 전체강의실` | 센텀 | regular-may |
| 2 | `(센텀) 여름방학시간표` | 센텀 | summer-2026 |
| 3 | `(사직) 5월 강의실` | 사직 | regular-may |
| 4 | `(사직) 여름방학 시간표` | 사직 | summer-2026 |
| 5 | `수학강사별 시간표` | (자동) | (자동) |
| 6 | `영어강사별 시간표` | (자동) | (자동) |
| 7 | `국어강사별 시간표` | (자동) | (자동) |
| 8 | `탐구강사별 시간표` | (자동) | (자동) |

**시트의 탭 이름이 다르면 두 가지 선택지:**
- 시트 탭 이름을 위 표대로 변경 (권장)
- 또는 `scripts/sync-from-sheet.mjs` 13~20행의 `TABS` 배열을 수정

---

## 3. 동작 확인

### 수동 1회 실행 (시트 공개 직후)

```powershell
# 로컬에서 (Windows)
cd C:\학원자료\클래스픽
node --use-system-ca scripts/sync-from-sheet.mjs
# → db/courses.json 갱신, 콘솔에 "[sync] 8/8 tabs OK → ... courses" 출력
```

```bash
# GitHub Actions 수동 트리거 (어디서든)
gh workflow run sync-from-sheet
# 진행 상황 확인
gh run list --workflow=sync-from-sheet --limit 3
```

### 결과 검증

`db/courses.json`이 갱신되면:
- 총 강좌 수는 **150~280건** 범위가 정상 (시즌·강사별 시간표 중복 후)
- `lastSyncedAt` 필드가 방금 시각으로 갱신
- 라이브 사이트 푸터 "데이터 동기화" 줄에 `시트 자동 동기화 · 2026-05-30 17:30 KST` 식으로 표시

라이브 반영은 git push → Vercel 30초~1분 후.

---

## 4. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| `[fetch] ... HTML response (sheet not public yet?)` | 시트가 비공개 | 위 1단계 다시 |
| `[fetch] ... HTTP 400` | 탭 이름 불일치 | 위 2단계 체크리스트 |
| `FATAL: 0 courses parsed` | 시트 구조 변경 / 권한 누락 | 시트 공개 + 셀 포맷 확인 |
| 로컬에서 `unable to verify the first certificate` | Windows TLS 캐시 | `node --use-system-ca scripts/sync-from-sheet.mjs` |
| 강좌 수가 500건 이상 | dedupe 로직 버그 | `scripts/sync-from-sheet.mjs` 의 `dedupe()` 점검 |

---

## 5. cron 시간 (KST 기준)

- `cron: "0 0 */2 * *"` (UTC) = **매 이틀 09:00 (KST)**
- 1, 3, 5, 7… 일자 09:00에 자동 실행
- 시트를 수정한 직후 즉시 반영하려면: `gh workflow run sync-from-sheet`

---

## 6. 셀 작성 규칙 (대표님께서 시트 입력 시)

파서가 인식하는 패턴:

```
[prefix] 과목 / 클래스명-또는-학교반 / 요일 시간 / 강사이름T
```

**예시 (모두 인식됩니다):**

```
(센텀) 영어 / 부산외고2 / 토 09:00-13:00 / 윤훈관T
수학 / 해운대고1 / 토/일 10-13 / 이영환T
(센텀) 수학 / 부일고2 / 토일 13:00 - 16:00 / 김형석T
영어 / LEVEL2 문법 (연합2) / 화목 17:00-20:00 / 권정은T
(특강) 영어 / 고 1,2 통합 모의고사 / 금 14:00-17:00 / 정준호T
(사직) 수학 / 미적분1 클리닉 / 화목 19-22 / 차동우T
```

**prefix 태그 의미:**

| 태그 | 의미 |
|---|---|
| `(센텀)` | 교육인학원 센텀점 (기본값 — 생략 가능) |
| `(사직)` | 교육인학원 사직점 |
| `(특강)` | 방학특강·이벤트 (courseType=방학특강 자동 부여) |

**시간 표기 — 모두 인식:**
- `토 09:00-13:00`
- `토 9-13`
- `토일 10:00 - 13:00`
- `토/일 10-13`
- `화·목 17-20`

**강사 표기 — 마지막 부분에 `이름T` (`T`는 필수)**

---

## 7. 자동 동기화 흐름 (한 눈에)

```
[대표님] 시트 수정
   ↓
(다음 짝수 일자 09:00 KST 또는 수동 트리거)
   ↓
[GitHub Actions] node scripts/sync-from-sheet.mjs
   ↓
8개 탭 fetch + 파싱 + dedupe
   ↓
db/courses.json 갱신
   ↓
git diff 있으면 자동 commit + push
   ↓
[Vercel] main 브랜치 변경 감지 → 30초~1분 후 배포
   ↓
[classpick.vercel.app] 새 데이터 노출 + 푸터에 동기화 시각 표시
```

---

## 8. 약속

- 시트가 ground truth — 사이트 데이터는 시트를 따른다
- 학원이 정정·삭제 요청 시: 시트에서 해당 행 삭제 → 다음 동기화 또는 수동 트리거 → 라이브 반영 (영업일 1일 이내 약속 유지)
- 윤훈관 6개 화이트리스트는 별도 노출 필터에서 관리 (시트는 모든 데이터 보존)
